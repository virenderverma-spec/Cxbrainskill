{
  "name": "cs-intelligence-server",
  "version": "1.0.0",
  "description": "Backend server for Zendesk Copilot with Claude AI and CS skills",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test-skills": "node -e \"const {loadAllSkills, listAvailableSkills} = require('./lib/skill-loader'); console.log('Skills:', listAvailableSkills()); console.log('\\nLoaded content length:', loadAllSkills().length, 'chars');\""
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
