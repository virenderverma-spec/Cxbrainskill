ZENDESK_SUBDOMAIN=your-sandbox-subdomain
ZENDESK_EMAIL=admin@yourcompany.com
ZENDESK_API_TOKEN=your-api-token
ZENDESK_ENV=sandbox

# Brand detection (optional — falls back to 'Meow Mobile' if table doesn't exist)
DATABRICKS_BRAND_TABLE=rds-prod_catalog.cj_prod.customer_brand_mapping
DATABRICKS_BRAND_COLUMN=brand
